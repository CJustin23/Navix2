{
  "name": "navix2",
  "private": true,
  "workspaces": [
    "navix-app"
  ],
  "engines": {
    "node": "24.x"
  },
  "packageManager": "npm@9",
  "scripts": {
    "build": "npm --workspace navix-app run build",
    "dev": "npm --workspace navix-app run dev",
    "preview": "npm --workspace navix-app run preview",
    "lint": "npm --workspace navix-app run lint"
  }
}